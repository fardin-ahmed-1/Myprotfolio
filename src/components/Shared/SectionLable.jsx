import React from 'react'

const SectionLable = ({title}) => {
  return (
    <div>
        <h1 className='text-white font-semibold  text-5xl text-center'>{title}</h1>
    </div>
  )
}

export default SectionLable